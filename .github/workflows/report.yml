name: report

on:
  push:
    branches: [main]
    paths: ['data.sqlite', 'create_graphs.py', 'make_report.py', 'report.md', 'report.css',
            'template.html', '.github/workflows/report.yml']
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install gnuplot and pandoc
        run: sudo apt-get update -qq && sudo apt-get install -y -qq gnuplot-nox pandoc python3-yaml

      - name: Build plots and report
        run: |
          ./create_graphs.py --no-display
          ./make_report.py

      - name: Assemble the site
        run: |
          mkdir -p _site
          cp report.html _site/index.html
          cp -r pics _site/pics

      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
